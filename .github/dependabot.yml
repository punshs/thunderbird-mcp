# Automated dependency updates -- GitHub-native, no third-party app needed.
# https://docs.github.com/en/code-security/dependabot
#
# Two ecosystems matter for this repo:
#   - npm: dev dependencies (acorn, eslint, better-sqlite3, etc).
#     Production package.json `dependencies` is empty -- the extension
#     itself ships no npm deps.
#   - github-actions: pins on actions/checkout@vN, actions/setup-node@vN
#     in .github/workflows/*.yml. Without this, action major versions
#     silently drift and CI breaks on Node 20 -> Node 24 deprecation.
#
# Schedule weekly so the PR backlog stays small. Grouped by ecosystem so
# we don't get one PR per dep -- a single rollup PR per Monday morning.
# Open-PR cap keeps things sane if a release sprint goes quiet for a
# few weeks.
version: 2
updates:
  - package-ecosystem: npm
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Warsaw
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - npm
    commit-message:
      prefix: chore(deps)
      prefix-development: chore(deps-dev)
      include: scope
    groups:
      # eslint and its plugins move in lockstep -- group them (including
      # majors) into one PR so a bump like eslint 9 -> 10 doesn't arrive as
      # several separate, interdependent PRs.
      eslint:
        patterns:
          - "eslint"
          - "@eslint/*"
          - "eslint-plugin-*"
        update-types:
          - major
          - minor
          - patch
      # Single rollup PR per week for minor + patch bumps; other majors stay
      # individual so each one gets its own review.
      minor-and-patch:
        update-types:
          - minor
          - patch

  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: Europe/Warsaw
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions
    commit-message:
      prefix: chore(ci)
      include: scope
    groups:
      actions:
        patterns:
          - "*"